import { Layout } from 'antd';

const HeaderBar = () => {
    const { Header } = Layout;
    return (
        <Header
            style={{
                padding: 0,
                background: '#FFFFFF',
            }}
        >
        </Header>
    )
}

export default HeaderBar